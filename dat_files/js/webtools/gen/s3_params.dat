fields {

  // Parses credentials, config files in project secrets
  // to read access keys, regions
  //
  string profile;

  string bucket_name;

  // If nonempty, this path is prepended to each method's name argument
  //
  string folder_path;
}
