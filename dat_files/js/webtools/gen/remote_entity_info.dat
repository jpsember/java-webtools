fields {
  string label;
  string user = "root";
  string url;
  int port;
  string host;
  JSMap host_info;    // information provided by host

  // Default directory (must be absolute)
  File project_dir;
}
