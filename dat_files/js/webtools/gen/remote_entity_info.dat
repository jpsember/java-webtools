enum js.webtools.gen.OsType;

fields {
  string id;
  OsType os_type;
  string user;

  // Directory containing the project; the one containing the git repo (on the remote machine)
  //
  File project_dir;

  // These are dynamic elements that are filled in with results of ngrok API
  //
  ?string url;
  ?int port;
}
